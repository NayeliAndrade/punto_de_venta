export type Product = {
        id: string,
        sku: string,
        product: string,
        image_product: FileList,
        description: string,
        unit_measure: string,
        VAT: number,
        price: number,
        cost: number,
        data_expiration: string
}