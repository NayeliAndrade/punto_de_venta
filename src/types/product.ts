export type product = {
        id: string,
        sku: string,
        product: string,
        image_product: FileList,
        description: string,
        unit_measure: number,
        VAT: number,
        price: number,
        cost: number,
        data_expiration: string
}