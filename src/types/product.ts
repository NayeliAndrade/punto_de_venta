export type product = {
        id: string,
        sku: string,
        product: string,
        image_product: string,
        description: string,
        unit_measure: number,
        iva: number,
        price: number,
        cost: number,
        data_expiration: string
}